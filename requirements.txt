# Loosened from `yfinance==0.2.51` (Dec 2024). The 0.2.x pin failed silently
# in Docker, leaving every analyst report saying "DATA UNAVAILABLE". The 1.x
# series is API-compatible for what we use (Ticker, history, info).
pandas>=2.2.0
yfinance>=0.2.51
requests>=2.31.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
xhtml2pdf>=0.2.16          # scripts/generate_portfolio_pdf.py
