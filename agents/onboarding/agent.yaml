spec_version: "0.1.0"
name: onboarding
version: 0.1.0
description: Conversational financial-intake agent. Captures the user's goals, finances, and constraints one short question at a time, then writes memory/user_plan.md and generates RULES.md.
model:
  preferred: "amazon-bedrock:us.anthropic.claude-sonnet-4-5-20250929-v1:0"
tools: [cli, read, write, memory]
runtime:
  max_turns: 30
